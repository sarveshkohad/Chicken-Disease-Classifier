[project]
name = "mlops-pipeline"
version  = "0.0.1"
description = "Production grade MLOps pipeline for CNN Classification"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    {name="Sarvesh Kohad"}
]
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }

[tool.setuptools.packages.find]
where = ['.']
include = ['src*']
